=== IDEAS ===
- const

=== TODO ===
- sec-ignore(section names, and more section names to ignore)

- (add plus equals minus equals)

=== DONE ===
- Throw error for double variable decleration
- variable overwrite (re-assignement)
- String decleration
- flt decleration
- char decleration
- int decleration
- function calls
- mikas
- add pointers
- add derefferencers (&)
- more boolean logic (!=, ==, &&, ||, !, <, >, <=, >=)
- while
- re-asign vars with '?='
- for (for (int i = 10; i < 50) {code})
- arrays
- structs


=== C-FUNCTIONS ====

char* strcpy(char* dest, const char* src) {
    do {
        *dest++ = *src++;
    } while (*src != 0);
    return 0;
}



=== struct definition ===

struct test typedef {
    str addresse
    str name
    int hausnummer
}

(struct) test (*) TestStruct

test.addresse = "Xy"
test.name = "test name"
test.hausnummer = 21

int haus_nummer_kopie = test.hausnummer